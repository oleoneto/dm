schema: 1
name: CreateUsers
adapter: postgresql
changes:
  up: |
    CREATE TABLE users (
      id SERIAL,
      name varchar not null,
      username varchar unique not null,
      PRIMARY KEY (id)
    );
  down: |
    DROP TABLE users;
